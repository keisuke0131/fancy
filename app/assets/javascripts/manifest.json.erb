<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "Fancy -彼女なんて誰でもできる-",
  "short_name": "Fancy",
  "start_url": "/",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= image_path "icon.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#07051a",
  "background_color": "#07051a",
  "display": "standalone",
  "orientation": "portrait"
}
