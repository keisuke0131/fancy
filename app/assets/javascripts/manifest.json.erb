<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "Fancy",
  "short_name": "Fancy",
  "start_url": "/",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= asset_path "serviceworker-rails/icon-#{dim}.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#07051a",
  "background_color": "#07051a",
  "display": "standalone",
  "orientation": "portrait"
}
